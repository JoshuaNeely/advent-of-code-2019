import { calculate_fuel } from "src/fuel-calculation";

describe('calculate_fuel', function() {
  it('should do stuff', function() {
    const result = calculate_fuel();
    expect(result).toBe(42);
  });
});
